/*
 *
Crear una función que pida el ingreso de un entero y lo retorne.

 */

#include <stdio.h>
#include <stdlib.h>

 int entero (int);

int main(void) {

	setbuf(stdout, NULL);

	int numeroIngresado;

	printf ("ingrese un numero (entero).");
		scantf ( "%d",&numeroIngresado);

 numeroIngresado = entero (numeroIngresado);
	return 0;

} // fin de la funcion main.


 int entero (int numero){

	 int respuesta;

		 respuesta = numero;


	 return respuesta;
 }
